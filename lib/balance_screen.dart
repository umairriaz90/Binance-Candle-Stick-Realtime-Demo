import 'package:flutter/material.dart';

class BalanceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Balance Display'),
      ),
      body: Center(
        child: Text('This is the Balance Screen'),
      ),
    );
  }
}
