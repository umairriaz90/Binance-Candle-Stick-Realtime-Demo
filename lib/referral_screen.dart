import 'package:flutter/material.dart';

class ReferralScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Referral System'),
      ),
      body: Center(
        child: Text('This is the Referral Screen'),
      ),
    );
  }
}
