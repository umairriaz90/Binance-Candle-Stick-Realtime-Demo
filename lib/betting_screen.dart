import 'package:flutter/material.dart';

class BettingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Crypto Betting'),
      ),
      body: Center(
        child: Text('This is the Crypto Betting Screen'),
      ),
    );
  }
}
